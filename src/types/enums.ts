export enum status_appointment {
  Canceled = 'Canceled',
  Pending = 'Pending',
  Confirmed = 'Confirmed',
  Completed = 'Completed',
  Deleted = 'Deleted',
}

export enum status_comment {
  Active = 'Active',
  Pending = 'Pending',
  Inactive = 'Inactive',
  Deleted = 'Deleted',
}

export enum status_user {
  Active = 'Active',
  Pending = 'Pending',
  Inactive = 'Inactive',
  Deleted = 'Deleted',
}
